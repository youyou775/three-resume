name: Build three-resume Next.js
on: push
jobs: 
  build-project:
    name: Build project
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: install npm dependencies
        run: npm ci
      - name: build assets
        run: npm run build